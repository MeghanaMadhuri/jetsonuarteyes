# Optional edge voice stack (ASR + LLM router + TTS).
# Install in the kiosk venv or a dedicated venv on Jetson:
#   pip install -r requirements-voice-edge.txt
#
# System packages (apt): alsa-utils espeak-ng ffmpeg ollama
# Models: ollama pull gemma2:2b  (or llama3.2:3b-instruct-q4_K_M)
#         Whisper CTranslate2 under models/whisper-tiny/ (see scripts/install-voice-edge.sh)

fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0
requests>=2.28.0
websockets>=11.0
faster-whisper>=1.0.0
huggingface_hub>=0.20.0
ctranslate2>=4.0.0
torch>=2.0.0
